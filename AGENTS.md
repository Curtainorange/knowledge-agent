# 注意事项

1. 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
2. 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。
3. 每次完成一个开发事项后，顺手更新 `dev_logs/` 下当天日期日志文件（YYYY-MM-DD.md）的「今日完成」列表；尚未完成但需要跟进的内容放入「待办事项」。当天日志不存在则先用 `dev_logs/TEMPLATE.md` 模板创建（填充星期）。