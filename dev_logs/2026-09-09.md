# 开发日志 · 2026-09-09（星期三）

## 今日完成

- [x] 搭建开发日志系统：创建 `dev_logs/` 目录、日志模板 `TEMPLATE.md`、说明 `README.md`，并设置每晚定时任务自动记录当天完成/待办事项
- [x] 模型供应商从 Anthropic Claude 切换为国产 DeepSeek，统一改写五份文档（认知副驾.md、需求分析、架构设计、系统设计 .md/.html）为「单一模型 deepseek-v4-flash + 每请求 reasoning 开关」架构，并同步更新 HTML 交付版
- [x] 搭建 P0 可运行骨架：`app/` 模块化单体 + LLM 网关（策略路由 → Provider → 重试 → 成本埋点）+ 领域模型与仓储（含 Claim/LearningEvent/CostLog）+ 最小聊天 API + Alembic 脚手架
- [x] P0 验证通过：`pytest` 全绿（7 项，全程 Mock 不触真实 API）；本地 uvicorn 冒烟 通过，`X-Request-Id / request_id` 贯穿 API→网关，`request_id==响应值`，成本日志落库
- [x] **L1 认知挖掘垂直切片（MVP）落地**：
  - [x] 语义检索层：`embedding` 抽象（本地 BGE 真语义 / 确定性 hash 兜底，conftest 强制 hash 不触网）+ 入库 BLOB 存向量 + InMemory 余弦 + 关键词兜底（可靠-4）
  - [x] L1 闭环编排：`l1_orchestrator` 复用了 `Conversation.state(澄清状态机)` + `messages[]`，召回候选 → 网关 `l1_mining`（reasoning=on，ADR-11 结构化）→ located 交付条目 + 阅读进度提醒 / clarify 追问（回合上限 3 兜底）
  - [x] API：`POST /knowledge/items` 手动录入（D4）+ `POST /l1/mine` 发起/承接挖掘（D3）；`main.py` 挂载新路由
  - [x] 迁移：`migrations/versions/0001` 增 `knowledge_items.embedding/embed_status` 列（dev.db 走 create_all 重新生成）
- [x] **MVP 验证通过**：`pytest` 全绿（25 项）；HTTP 冒烟 闭环跑通——录入 → `clarifying` → 回答后续接 → `located` 返回命中条目 + 「尚未读完」提醒，`cost_logs` 逐次落库（2 行 = 2 次 LLM 调用），`request_id` 贯穿；无 fastembed 时 `embed_failed` 自动降级关键词召回不阻断落库

## 待办事项

- [ ] `deepseek-v4-flash` reasoning 切换与结构化输出的精度兼容性，待接入真实 API 后实测校准（回填模型字段与单价到 .env）
- [ ] 真实语义冒烟：本机装 `fastembed` + 首次下载 `BAAI/bge-small-zh-v1.5`，用真实 DeepSeek 校验 L1 结构化路由精度与 cost 实测
- [x] **真实链路接入 + 冒烟通过**（用户提供 DeepSeek `sk-` key）：
  - [x] 内网环境装 `fastembed 0.8.0`（PyPI 官方源 onnxruntime 下载卡死 → 换清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 秒下）
  - [x] 首次加载 BGE：HF 官方源连接超时 → 走 `hf-mirror.com` + `HF_HUB_DISABLE_XET=1`（fastembed 新版用 Xet 绕过镜像直接连 HF CDN 会 401）；模型已缓存
  - [x] **修 2 个真实 bug**：① `fastembed 0.8` `embed()` 返回生成器，`_ensure` 用 `[0]` 下标崩 → 改 `list(...)`；② openai SDK 不认 `reasoning` 顶层参数 → 改 `extra_body={"reasoning":True,...}`（实测服务器接受）
  - [x] 真实端到端验证：模糊线索→ `located` 直接命中「数据库索引实践」（BGE 512 维真语义 + DeepSeek 结构化路由一次定位），`cost_logs` 落库（`l1_mining / deepseek-v4-flash / reasoning=1 / 405+87 tokens`）
- [x] 国内下载配置收敛进 `.env`/`config`：新增 `HF_ENDPOINT`、`HF_HUB_DISABLE_XET`，`BGEHesEmbedding._ensure()` 在首载前写入 os.environ（shell 已设则优先），无需手动 export
- [ ] 待办：`.docx` 交付版后续由用户在 Word 手动重新生成同步

## 明日计划

- [ ] 真实违规单价回填：实测 cost 采样后把 `DEEPSEEK_PRICE_*` 由占位 1.0/4.0 校准为实际单价
- [ ] 换 pgvector / 精度调参，或推进 L2 冲突检测 / 知识源连接器

## 备注

- Windows 中文控制台 GBK 曾导致日志乱码，已在 `core/logging.py` 强制 UTF-8 输出修复。
- `uvicorn[standard]` 触发 pip 解析器回溯，P0 已简化为 `uvicorn>=0.30`。
- fastembed 升级到 protobuf 7.36，会与 anaconda 自带 streamlit(<protobuf6) 冲突，本项目 deps 不含 streamlit，不受影响。
- HF 模型缓存路径按 `HF_ENDPOINT` 命名空间隔离，换镜像/直连可能二次下载；`HF_HUB_DISABLE_XET=1` 必须保留（否则大文件仍走被墙的 Xet CDN）。