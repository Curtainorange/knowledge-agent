# 开发日志 · 2026-09-10（星期四）

## 今日完成

- [x] **知识条目 CRUD 补齐（L1 可用化 1/3）**：此前只有 `POST /items`，知识「只进不出」（录进去查不到、改不了、删不掉）
  - [x] 仓储层：`KnowledgeRepository` 增 `page_active`（分页）/ `count_active`（总数）/ `soft_delete` / `apply_update`（局部更新，返回「可检索文本是否变化」）；`list_active` 保持全量语义供检索使用，分页另开 `page_active` 以免误伤召回
  - [x] 服务层：`IngestionService` 增 `update_knowledge`（**文本变更才重算 embedding**，只改进度不触发无谓向量开销）/ `delete_knowledge`
  - [x] API：`GET /api/v1/knowledge/items`（分页，默认 20 上限 100，只给摘要不回灌全文）、`GET /items/{id}`（详情全文）、`PATCH /items/{id}`（title/content/tags/read_progress）、`DELETE /items/{id}`（软删，保留原文）
  - [x] 越权防护落到 HTTP 层：仓储 `PermissionError` → `403`（此前会冒泡成 500）；条目不存在 → `404`
- [x] **L1 定位交付物加厚（2/3）**：`LocatedItem` 增 `snippet`（复用新增的 `KnowledgeItem.snippet` 定长摘要）与 `embed_status`，定位后无需二次请求即可展示
- [x] **修复阅读提醒恒真的假信号（3/3）**
  - [x] `read_hint` 先前只 `logger.info`、**API 响应里根本没有这个提醒**；现改为 `L1Result` 字段并在 `/l1/mine` 返回
  - [x] `read_progress` 此前创建时写死 0.0、全库无更新点 → 字段是死的、提醒恒真；现可通过 `PATCH` 写入，提醒按真实值分档：未读「还没开始读」/ 0<p<1「已读 N%」/ 读完静默
- [x] 验证：`pytest` 全绿 **37 项**（原 25 + 新增 12，无回归）
- [x] **真实链路端到端冒烟通过**（真实 BGE 512 维 + DeepSeek，临时库 `smoke_tmp.db`，验证后已清理）：
  - [x] 改正文触发真实 BGE 重算，模糊线索「之前存的那段讲数据库查询优化的东西」仍精准命中改写后条目（证明重算向量生效）
  - [x] 只改进度不触发重算；越权访问 403；软删后列表消失、详情 404、行仍保留；`read_hint` 实测返回「这条已读 50%，还没读完」

## 待办事项

- [ ] 接入实测后回填真实 $DeepSeek$ 单价：`DEEPSEEK_PRICE_INPUT/OUTPUT_PER_1M` 目前仍为占位 1.0/4.0，需按 `cost_logs` 实测采样校准
- [ ] `.docx` 交付版（架构/需求/系统设计）后续由用户在 Word 手动重新生成同步
- [ ] **L1 仍无界面**：只能 curl / FastAPI docs 调用，属「开发者可用」而非「用户可用」
- [ ] `read_progress` 现靠手动 `PATCH`，尚未接真实阅读/复习事件源（`LearningEvent` 驱动）
- [ ] 检索仍为全量 `list_active` + 内存余弦，条目规模上去后需迁 pgvector（P2）
- [ ] 会话无重置出口；`_question_count` 统计所有 assistant 消息，混用 `/chat` 会污染回合计数、提前触发兜底

## 明日计划

- [ ] 做最小可聊界面（streamlit 或单页 HTML），让 L1 真正可自测
- [ ] 或推进 L2 冲突检测 —— 当前 L1 实质仍是「语义搜索 + 追问」，尚未产出用户不知道的东西（关联 / 冲突 / 认知空白）

## 备注

- 09-09 已跑通真实链路：真语义 L1「模糊线索→located 命中」，`deepseek-v4-flash` reasoning=on + BGE 512 维，模型已走 HF 镜像缓存。
- 待办中的价格校准需要累计若干次调用后再做，避免单次采样失真。
- 分页排序加 `id` 作二级键：`created_at` 只精确到秒，同秒录入时分页顺序不确定会让翻页结果抖动。
- 真实冒烟可用临时库隔离，避免污染 `dev.db`：`DATABASE_URL="sqlite:///./smoke_tmp.db" python -m uvicorn app.main:app`（`*.db` 已在 .gitignore）。