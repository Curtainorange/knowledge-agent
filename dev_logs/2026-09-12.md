# 开发日志 · 2026-09-12（星期六）

## 今日完成

- [x] **鉴权落地：把「请求头自选身份」换成「Bearer JWT」**（系统设计 9.3.1）
  - [x] `app/core/security.py`：密码用 PBKDF2-HMAC-SHA256 加盐哈希，存储格式 `pbkdf2_sha256$iterations$salt$hash`（迭代数写进串内，将来调高全局迭代数不会锁死老用户）；JWT 签发/校验走 PyJWT，解码时**显式传算法白名单**防 alg 混淆，并区分 access / refresh 类型
  - [x] `User` 实体增加 `username`（唯一索引）/ `password_hash`，配套迁移 `0002_add_user_credentials`
  - [x] 新增 `POST /api/v1/auth/register`（注册即下发令牌）、`POST /api/v1/auth/login`、`POST /api/v1/auth/refresh`、`GET /api/v1/auth/me`
  - [x] `deps.get_user_id` 改为解析 Bearer token 并注入 `user_id` —— 既有端点原先就依赖这一项，因此**无需逐个改造即自动获得鉴权**，改动面最小
  - [x] 令牌失效/缺失统一 `401` 并带 `WWW-Authenticate: Bearer`；refresh token 不能当访问令牌用（否则长期凭证绕过短期过期策略）
- [x] **堵住既有身份伪造缺口**：旧实现直接信任 `X-User-Id` 请求头，任何人改个头就能读写他人数据；该头现已不再具备决定身份的能力，并加了回归测试锁定
- [x] 界面接入登录态：登录/注册视图、令牌存 localStorage、请求自动携带 Authorization、遇 401 自动清理并回到登录页；顶部改为「当前账号 + 退出」（移除可任意填写的用户切换框）
- [x] 防账号枚举：登录失败不区分「账号不存在」与「密码错误」，且账号不存在时仍走一次哈希校验以拉平耗时
- [x] 测试：新增 `tests/test_auth.py`（14 项），既有用例全部切到 token 认证（并顺带修正了三处直接查库断言、原先用写死字符串当 user_id 的用例）；`pytest` **53 项全绿**（原 39 + 新增 14，无回归）
- [x] 端到端冒烟通过（21 项全 OK）：未认证 401 / 伪造 `X-User-Id` 失效 / 注册登录 / 防枚举 / refresh 换发 / 跨用户猜 id 取数 403 / 带令牌 L1 挖掘

## 待办事项

- [ ] **向量模型缓存已丢失且当前网络无法下载**（详见备注），条目降级为关键词召回；需在有网环境重新拉取
- [ ] 响应体尚未统一为设计文档 §8.2 的 `{code, message, data, request_id}` 包装（现沿用 `detail`），前端与测试按现状适配
- [ ] 未做登录失败限流 / 账号锁定，暴力破解目前没有成本
- [ ] 缺「修改密码」「注销账号」入口；令牌无吊销机制（签发后只能等过期）
- [ ] 接入实测后回填真实 DeepSeek 单价（沿用 09-10 待办）
- [ ] `.docx` 交付版需在 Word 手动重新生成同步

## 明日计划

- [ ] 恢复向量模型，或评估替代方案（ModelScope 仅有 PyTorch 版，fastembed 需要 ONNX）
- [ ] 或推进 L2 冲突检测

## 备注

- **向量模型问题定位**：`~/.cache/huggingface` 只剩 Xet 日志残渣，模型文件确已不存在；重下时 hf-mirror.com 返回 502、直连 huggingface.co 超时 20s 无响应，ModelScope 上只有 PyTorch 版（`AI-ModelScope/bge-small-zh-v1.5`），没有 fastembed 所需的 ONNX 版（`Qdrant/bge-small-zh-v1.5-onnx-Q` 在 ModelScope 为 404）。
  **代码本身无误** —— 降级链路实测有效：向量不可用时条目照常入库（`embed_failed`），L1 仍能靠关键词通道命中目标并返回摘要与阅读提醒。
- 沙箱环境注入了 `http_proxy / https_proxy`（`127.0.0.1:50574`），该代理可通 pypi 与 modelscope，但会拦截 HF 域名（表现为服务端收到 502）；自行启动服务且网络正常时可直接下载。
- 旧 `dev.db` 的 `users` 表缺新列，已删除重建（删除前确认 9 张表均为 0 行，无数据损失）。
- 前端踩到一个 ASI 隐患：以 `[` 开头的续行会被解析成对上一行结果的索引访问，**语法检查查不出来、只在运行时崩**，已改成具名函数调用。
- 鉴权选型：JWT 用已安装的 PyJWT（不自造轮子，签名校验、过期、算法混淆这些边界不值得手写）；密码哈希用标准库 PBKDF2，避免再引入 passlib/bcrypt 的版本与 72 字节截断坑。
